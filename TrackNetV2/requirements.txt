# Runtime dependencies for TrackNetV2 inference.
#
# The previous file was a full Linux/CUDA pip freeze. It pinned NVIDIA
# transitive wheels such as nvidia-cudnn-cu12==8.9.2.26, which are not
# portable to Windows/Python 3.13 and are resolved automatically by PyTorch
# when a matching wheel is available.

torch>=2.8,<2.11
torchvision>=0.23,<0.26

numpy>=2.1
opencv-python>=4.10
Pillow>=10.4
PyYAML>=6.0.2
tqdm>=4.66
torchsummary>=1.5.1

# Used by monitor_usage.py.
psutil>=5.9.8
